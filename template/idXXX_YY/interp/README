Place the relevant interpretation files (powerpoint presentations (*.pptx, *.ppt), images (*.png, *.jpg), ...)) here.
These files will not be processed automatically, so their naming and formats do not have to follow any strict conventions.